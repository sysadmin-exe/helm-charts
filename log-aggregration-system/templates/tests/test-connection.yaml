apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "log-aggregation-system.fullname" . }}-test-connection"
  labels:
    {{- include "log-aggregation-system.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "log-aggregation-system.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
